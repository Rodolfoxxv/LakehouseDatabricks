resources:
  schemas:
    raw:
      name: raw
      catalog_name: ${var.catalog}
    bronze:
      name: bronze
      catalog_name: ${var.catalog}
    silver:
      name: silver
      catalog_name: ${var.catalog}
    gold:
      name: gold
      catalog_name: ${var.catalog}

  volumes:
    landing:
      name: landing
      catalog_name: ${var.catalog}
      schema_name: ${resources.schemas.raw.name}
      volume_type: MANAGED
